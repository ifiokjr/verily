name: verily_ui
description: Shared UI theme and components for Verily applications.
version: 0.1.0
publish_to: 'none' # Prevent accidental publishing

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

resolution: workspace